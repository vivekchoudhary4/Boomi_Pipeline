@Library('pipeline') _

pipeline {
	agent any
	
	parameters { string defaultValue:'',description:'',name:'Component_ID'
		   }

	stages {
	
		stage('Build') {
			steps {
				echo '*************************Build Started*************************************'
				}
			}
		}
}
